source "https://rubygems.org"

# Use a mesma versão do GitHub Pages para evitar incompatibilidade
# entre o que roda local e o que o GitHub publica.
gem "github-pages", group: :jekyll_plugins

# Necessário em Ruby 3+ (o github-pages não traz mais por padrão)
gem "webrick"
